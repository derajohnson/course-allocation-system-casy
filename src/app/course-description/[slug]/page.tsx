import CourseDescription from "@/components/modules/home/CourseDescription";

const Slug = ({ params }: { params: { slug: string } }): React.JSX.Element => {
  return <CourseDescription />;
};

export default Slug;
